# --------------------------------------------------------------------------------------------------
# Generate a new migration with:
# bin/console doctrine:migrations:diff --em=default --configuration=var/plugins/AakSamlBundle/Migrations/aak-saml.yaml
#
# Execute all migrations with:
# bin/console kimai:bundle:tasks:install
# bin/console doctrine:migrations:migrate --em=default --configuration=var/plugins/AakSamlBundle/Migrations/aak-saml.yaml
# --------------------------------------------------------------------------------------------------

table_storage:
  table_name: bundle_migration_aak_saml

migrations_paths:
  KimaiPlugin2\AakSamlBundle\Migrations: var/plugins/AakSamlBundle/Migrations